# 10K Audit Plan

This branch adds a deterministic 10,000-iteration invariant stress harness and makes the static exhaustive collector publish a non-zero verdict when either static audit or deterministic stress fails.

This is not a substitute for physical-device or media E2E verification. Those remain separate evidence classes.
