name: Sequence 12 - Verify Auto Fix
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  sequence-12:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Verify Sequence 12
        run: bash .github/scripts/sequence-stage-check.sh 12
