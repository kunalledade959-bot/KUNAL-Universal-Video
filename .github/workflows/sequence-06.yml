name: Sequence 06 - Operate Target
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  sequence-06:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Verify Sequence 06
        run: bash .github/scripts/sequence-stage-check.sh 6
