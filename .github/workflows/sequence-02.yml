name: Sequence 02 - Mobile Connection
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'sequence-test-trigger.txt'
permissions:
  contents: read
jobs:
  sequence-02:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Verify Sequence 02
        run: bash .github/scripts/sequence-stage-check.sh 2
