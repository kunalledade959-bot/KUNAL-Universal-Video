name: Sequence 08 - Scene Plan
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  sequence-08:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Verify Sequence 08
        run: bash .github/scripts/sequence-stage-check.sh 8
